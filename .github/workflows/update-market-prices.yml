name: Update Market Prices

on:
  schedule:
    # Run daily at 00:00 UTC (approximately the same time each day)
    - cron: '0 0 * * *'
  workflow_dispatch:
    # Allow manual trigger for testing
    
permissions:
  contents: write

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml
      
      - name: Run market price collector
        id: collect
        run: python scripts/update_market_prices.py
        env:
          PYTHONUTF8: 1
      
      - name: Check for changes
        id: git-check
        run: |
          git add ram-prices.json
          if git diff --staged --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Commit and push if changed
        if: steps.git-check.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -m "Update market prices $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
      
      - name: Summary
        run: |
          echo "## Price Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** Completed" >> $GITHUB_STEP_SUMMARY
          echo "- **Changes detected:** ${{ steps.git-check.outputs.has_changes }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.git-check.outputs.has_changes }}" = "true" ]; then
            echo "- **Action:** Committed and pushed updated prices" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Action:** No meaningful price changes - skipped commit" >> $GITHUB_STEP_SUMMARY
          fi
